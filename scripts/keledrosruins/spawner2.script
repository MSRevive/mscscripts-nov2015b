{
	name_unique spawner2
}

#include keledrosruins/spawner_base

{ spawn_undead

	
	if ( TWO_IS_DEAD == 1 ) createnpc monsters/anim_archer $get(ent_me,origin) $get(ent_me,id)
	callexternal ent_lastcreated two_was_summoned
}
